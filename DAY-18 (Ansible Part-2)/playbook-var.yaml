---
- hosts: devops
  user: gfg
  become: yes
  connection: ssh
  vars:
    currstate: present
  tasks:

    - name: Installation HTTPD
      action: yum name=httpd state='{{currstate}}'
    - name: Install Git
      action: yum name=git state='{{currstate}}'
